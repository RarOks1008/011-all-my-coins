import { CustomvaluePipe } from './customvalue.pipe';

describe('CustomvaluePipe', () => {
  it('create an instance', () => {
    const pipe = new CustomvaluePipe();
    expect(pipe).toBeTruthy();
  });
});
