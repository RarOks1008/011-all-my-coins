import { CustomyearPipe } from './customyear.pipe';

describe('CustomyearPipe', () => {
  it('create an instance', () => {
    const pipe = new CustomyearPipe();
    expect(pipe).toBeTruthy();
  });
});
